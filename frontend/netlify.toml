# ============================================================
# Zen-gineering App — Configuration Netlify
# ============================================================

[build]
  command = "npx vite build"
  publish = "dist"

# ── SPA fallback (React Router) ───────────────────────────────
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
