{
  "name": "zengineering-v4.1",
  "version": "4.1.0",
  "description": "Zengineering V4.1 — IA Orchestrator + Full Project Management",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
